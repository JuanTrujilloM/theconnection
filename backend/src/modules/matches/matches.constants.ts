// HU-06: exactly 3 places are suggested and the user must pick at least 2.
// Shared by the service (generation) and the DTO (validation) so they agree.
export const SUGGESTION_COUNT = 3;
export const MIN_VENUE_SELECTION = 2;
